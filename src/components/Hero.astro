---
import { ArrowRight, Star, Sparkles, Heart } from 'lucide-react';
---

<section id="inicio" class="relative min-h-screen flex items-center justify-center pt-16">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-10">
    <div class="absolute top-20 left-20 w-32 h-32 bg-yellow-400 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute top-40 right-32 w-24 h-24 bg-yellow-500 rounded-full blur-2xl animate-pulse" style="animation-delay: 1s;"></div>
    <div class="absolute bottom-32 left-1/3 w-20 h-20 bg-yellow-600 rounded-full blur-xl animate-pulse" style="animation-delay: 2s;"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 max-w-7xl mx-auto px-6">
    <div class="grid lg:grid-cols-2 gap-16 items-center">
      <!-- Text Content -->
      <div class="text-center lg:text-left">
        <!-- Badge -->
        <div class="inline-flex items-center space-x-2 bg-slate-800/50 backdrop-blur-sm border border-yellow-500/30 rounded-full px-4 py-2 mb-8">
          <Sparkles class="w-4 h-4 text-yellow-400" />
          <span class="text-sm text-yellow-400 font-medium">Importados directamente desde Dubai</span>
        </div>

        <!-- Main Heading -->
        <h1 class="text-5xl lg:text-6xl font-bold text-slate-100 mb-6 leading-tight">
          Fragancias <span class="text-gradient">Orientales</span><br>
          <span class="text-slate-200">Auténticas</span>
        </h1>

        <!-- Subtitle -->
        <p class="text-xl text-slate-300 mb-8 max-w-2xl mx-auto lg:mx-0 leading-relaxed">
          Descubre la elegancia de las fragancias orientales más exclusivas, 
          importadas directamente desde Dubai para conquistar tu esencia.
        </p>

        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start mb-12">
          <a href="#fragancias" class="btn-primary inline-flex items-center justify-center space-x-2 group">
            <span>Ver Fragancias</span>
            <ArrowRight class="w-5 h-5 group-hover:translate-x-1 transition-transform" />
          </a>
          
          <a href="https://wa.me/573001234567?text=Hola%20DAR,%20me%20interesa%20conocer%20más%20sobre%20sus%20fragancias" 
             target="_blank" 
             rel="noopener noreferrer"
             class="btn-secondary inline-flex items-center justify-center space-x-2">
            <span>Consultar WhatsApp</span>
            <Heart class="w-5 h-5" />
          </a>
        </div>

        <!-- Trust Indicators -->
        <div class="flex flex-wrap justify-center lg:justify-start gap-8 text-sm text-slate-400">
          <div class="flex items-center space-x-2">
            <div class="w-2 h-2 bg-yellow-500 rounded-full"></div>
            <span>100% Auténticos</span>
          </div>
          <div class="flex items-center space-x-2">
            <div class="w-2 h-2 bg-yellow-500 rounded-full"></div>
            <span>Importación Directa</span>
          </div>
          <div class="flex items-center space-x-2">
            <div class="w-2 h-2 bg-yellow-500 rounded-full"></div>
            <span>Envío a Todo Colombia</span>
          </div>
        </div>
      </div>

      <!-- Hero Image -->
      <div class="relative">
        <div class="relative mx-auto w-80 h-96">
          <!-- Main Perfume Bottle -->
          <div class="absolute inset-0 bg-gradient-to-b from-slate-700 via-slate-600 to-slate-800 rounded-2xl shadow-2xl transform rotate-3"></div>
          <div class="absolute inset-2 bg-gradient-to-b from-yellow-500 to-yellow-600 rounded-xl shadow-xl"></div>
          <div class="absolute top-4 left-1/2 transform -translate-x-1/2 w-16 h-16 bg-slate-800 rounded-full border-4 border-yellow-400"></div>
          <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 w-24 h-8 bg-slate-800 rounded-full"></div>
          
          <!-- Floating Elements -->
          <div class="absolute -top-4 -right-4 w-8 h-8 bg-yellow-400 rounded-full animate-pulse"></div>
          <div class="absolute -bottom-4 -left-4 w-6 h-6 bg-yellow-500 rounded-full animate-pulse" style="animation-delay: 1s;"></div>
          <div class="absolute top-1/2 -left-8 w-4 h-4 bg-yellow-600 rounded-full animate-pulse" style="animation-delay: 2s;"></div>
        </div>
        
        <!-- Decorative Elements -->
        <div class="absolute top-10 -left-10 w-20 h-20 border border-yellow-400/30 rounded-full animate-spin" style="animation-duration: 20s;"></div>
        <div class="absolute bottom-10 -right-10 w-16 h-16 border border-yellow-500/30 rounded-full animate-spin" style="animation-duration: 15s; animation-direction: reverse;"></div>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <div class="w-6 h-10 border-2 border-yellow-400 rounded-full flex justify-center">
      <div class="w-1 h-3 bg-yellow-400 rounded-full mt-2 animate-pulse"></div>
    </div>
  </div>
</section>